# Backend API base URL.
# Local dev: leave unset, defaults to http://127.0.0.1:8000
# Render deploy: set to your backend service's public URL, e.g.
#   https://voiceforge-ops-backend.onrender.com
VITE_API_BASE=
